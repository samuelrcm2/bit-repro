export type AvatarThemeModeConfig = {
  avatar: {
    colors: {
      blue: string;
      purple: string;
      pink: string;
      red: string;
      orange: string;
      lightgreen: string;
      green: string;
      default: string;
    };
  };
};
